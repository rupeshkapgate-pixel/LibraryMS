[tool.ruff]
line-length = 120

exclude = [
    ".git",
    ".venv",
    "__pycache__",
    ".pytest_cache",
    "*/proto_generated/*",
    "*/grpc_clients/*",
]

[tool.pytest.ini_options]
asyncio_mode = "strict"