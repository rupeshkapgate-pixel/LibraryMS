"use client";
import Link from "next/link"; import { useMemo, useState } from "react"; import { useQuery } from "@tanstack/react-query"; import { RotateCcw, Search } from "lucide-react"; import { lendingApi } from "@/lib/api"; import { EmptyState, ErrorState, LoadingSkeleton, PageHeader, Pagination, Select } from "@/components/ui"; import LendingStatusBadge from "@/components/library/LendingStatusBadge"; import { daysRemaining, formatCurrency, formatDate, shortId } from "@/lib/utils"; import type { LendingRecord } from "@/types"; import { getErrorMessage } from "@/lib/error";
export default function BorrowedBooksPage(){const[page,setPage]=useState(1);const[search,setSearch]=useState("");const[filter,setFilter]=useState("all");const q=useQuery({queryKey:["lending","borrowed",page],queryFn:()=>lendingApi.listBorrowed(page,20).then(r=>r.data)});const rows=useMemo(()=>{let list=q.data?.data??[];const s=search.toLowerCase();if(s)list=list.filter(r=>[r.book_title,r.member_name,r.member_email,r.id].some(v=>v?.toLowerCase().includes(s)));if(filter==="overdue")list=list.filter(r=>r.status==="OVERDUE"||daysRemaining(r.due_date)<0);if(filter==="current")list=list.filter(r=>r.status==="BORROWED"&&daysRemaining(r.due_date)>=0);return list},[q.data,search,filter]);function timeLabel(r:LendingRecord){const d=daysRemaining(r.due_date);if(r.status==="RETURNED")return "Returned";if(d<0)return `${Math.abs(d)} days overdue`;if(d===0)return "Due today";return `${d} days remaining`}return <div><PageHeader title="Borrowed Books" description="Track current and overdue lending records."/><div className="card mb-5 grid gap-3 p-4 md:grid-cols-[1fr_220px]"><div className="relative"><Search className="absolute left-3 top-3 text-slate-400" size={17}/><input className="input pl-10" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search book, member, email, record…"/></div><Select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">All records</option><option value="current">Current</option><option value="overdue">Overdue</option></Select></div><div className="card overflow-hidden">{q.isLoading?<LoadingSkeleton rows={7}/>:q.isError?<ErrorState message={getErrorMessage(q.error,"Unable to load borrowed records")}/>:!rows.length?<EmptyState title="No borrowed books" message="Active lending records will appear here."/>:<><div className="table-container rounded-none border-0"><table><thead><tr><th>Book</th><th>Member</th><th>Borrowed Date</th><th>Due Date</th><th>Days Remaining / Overdue</th><th>Status</th><th className="text-right">Actions</th></tr></thead><tbody>{rows.map((r:LendingRecord)=><tr key={r.id}><td><div className="font-semibold text-slate-950 dark:text-white">{r.book_title??shortId(r.book_id)}</div><div className="font-mono text-xs text-slate-400">{shortId(r.book_id)}</div></td><td><div>{r.member_name??shortId(r.member_id)}</div><div className="text-xs text-slate-400">{r.member_email??"—"}</div></td><td>{formatDate(r.borrowed_at)}</td><td>{formatDate(r.due_date)}</td><td className={daysRemaining(r.due_date)<0?"font-semibold text-red-600":"font-medium text-slate-700"}>{timeLabel(r)}</td><td><LendingStatusBadge status={r.status}/>{r.fine_amount>0&&<div className="mt-1 text-xs font-semibold text-red-600">{formatCurrency(r.fine_amount)}</div>}</td><td className="text-right"><Link href="/lending/return" className="btn-secondary px-3 py-2"><RotateCcw size={15}/>Return</Link></td></tr>)}</tbody></table></div><Pagination page={q.data!.pagination.page} totalPages={q.data!.pagination.total_pages} totalCount={q.data!.pagination.total_count} pageSize={q.data!.pagination.page_size} onPage={setPage}/></>}</div></div>}
