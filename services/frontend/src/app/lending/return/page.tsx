"use client";
import { useMemo, useState } from "react"; import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query"; import toast from "react-hot-toast"; import { CheckCircle2, RotateCcw, Search } from "lucide-react"; import { lendingApi } from "@/lib/api"; import { Button, EmptyState, ErrorState, LoadingSkeleton, PageHeader, Select } from "@/components/ui"; import LendingStatusBadge from "@/components/library/LendingStatusBadge"; import { daysOverdue, formatCurrency, formatDate, shortId } from "@/lib/utils"; import type { LendingRecord, ReturnResponse } from "@/types"; import { getErrorMessage } from "@/lib/error";
export default function ReturnBookPage(){const qc=useQueryClient();const[lendingId,setLendingId]=useState("");const[search,setSearch]=useState("");const[result,setResult]=useState<ReturnResponse|null>(null);const borrowed=useQuery({queryKey:["lending","borrowed",1,500],queryFn:()=>lendingApi.listBorrowed(1,500).then(r=>r.data)});const records=useMemo(()=>{const s=search.toLowerCase();return (borrowed.data?.data??[]).filter((r:LendingRecord)=>(r.status==="BORROWED"||r.status==="OVERDUE")&&(!s||[r.book_title,r.member_name,r.member_email,r.id].some(v=>v?.toLowerCase().includes(s))))},[borrowed.data,search]);const selected=records.find((r:LendingRecord)=>r.id===lendingId);const mutation=useMutation({mutationFn:()=>lendingApi.return({lending_id:lendingId}),onSuccess:res=>{setResult(res.data);toast.success("Book returned successfully");qc.invalidateQueries({queryKey:["lending"]});qc.invalidateQueries({queryKey:["books"]});qc.invalidateQueries({queryKey:["dashboard"]})},onError:e=>toast.error(getErrorMessage(e,"Return failed"))});if(result)return <div><PageHeader title="Return Book"/><div className="card max-w-xl p-6"><div className="flex gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-emerald-50 text-emerald-600"><CheckCircle2 size={26}/></div><div><h2 className="text-lg font-bold text-slate-950">Return confirmed</h2><p className="mt-1 text-sm text-slate-500">Fine: {result.fine_amount>0?formatCurrency(result.fine_amount):"None"}</p></div></div><dl className="mt-6 space-y-3 rounded-2xl bg-slate-50 p-4 text-sm"><div className="flex justify-between"><dt className="text-slate-500">Returned on</dt><dd className="font-semibold">{formatDate(result.record.returned_at)}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Overdue</dt><dd>{result.is_overdue?<span className="badge-red">{result.overdue_days} days</span>:<span className="badge-green">No</span>}</dd></div></dl><Button className="mt-6 w-full" onClick={()=>{setResult(null);setLendingId("")}}>Return Another Book</Button></div></div>;return <div><PageHeader title="Return Book" description="Find an active borrowed record and process return with fine visibility."/><div className="grid gap-6 lg:grid-cols-3"><div className="card p-6 lg:col-span-2"><div className="relative mb-4"><Search size={17} className="absolute left-3 top-3 text-slate-400"/><input className="input pl-10" placeholder="Search active borrow by book, member, email, record ID…" value={search} onChange={e=>setSearch(e.target.value)}/></div>{borrowed.isLoading?<LoadingSkeleton rows={5}/>:borrowed.isError?<ErrorState/>:!records.length?<EmptyState title="No active borrowed records" message="There are no current loans matching the search."/>:<div className="space-y-4"><div><label className="label">Active borrowed record *</label><Select value={lendingId} onChange={e=>setLendingId(e.target.value)}><option value="">Select record…</option>{records.map((r:LendingRecord)=><option key={r.id} value={r.id}>{r.book_title??shortId(r.book_id)} | {r.member_name??shortId(r.member_id)} | Due {formatDate(r.due_date)}</option>)}</Select></div><div><label className="label">Or enter Lending ID manually</label><input className="input font-mono" value={lendingId} onChange={e=>setLendingId(e.target.value)} placeholder="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"/></div><Button className="w-full" disabled={mutation.isPending} onClick={()=>{if(!lendingId)return toast.error("Select or enter lending record ID");mutation.mutate()}}><RotateCcw size={16}/>{mutation.isPending?"Processing…":"Process Return"}</Button></div>}</div><aside className="card p-5"><h3 className="font-bold text-slate-950">Borrowing details</h3>{selected?<dl className="mt-4 space-y-3 text-sm"><div><dt className="text-slate-500">Book</dt><dd className="font-semibold">{selected.book_title??shortId(selected.book_id)}</dd></div><div><dt className="text-slate-500">Member</dt><dd className="font-semibold">{selected.member_name??shortId(selected.member_id)}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Borrowed</dt><dd>{formatDate(selected.borrowed_at)}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Due</dt><dd>{formatDate(selected.due_date)}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Overdue</dt><dd>{daysOverdue(selected.due_date)>0?<span className="badge-red">{daysOverdue(selected.due_date)} days</span>:<span className="badge-green">No</span>}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Expected fine</dt><dd className="font-bold text-red-600">{formatCurrency(daysOverdue(selected.due_date)*10)}</dd></div><LendingStatusBadge status={selected.status}/></dl>:<EmptyState title="No record selected" message="Select a record to show return details and expected fine."/>}</aside></div></div>}
