"use client";
import { useState } from "react"; import type { MemberCreate, MembershipStatus } from "@/types"; import { Button, Input, Select } from "@/components/ui";
interface Props{initialData?:Partial<MemberCreate>;onSubmit:(data:MemberCreate)=>Promise<unknown>;submitLabel?:string;loading?:boolean}
export default function MemberForm({initialData,onSubmit,submitLabel="Save Member",loading}:Props){const[form,setForm]=useState<MemberCreate>({full_name:initialData?.full_name??"",email:initialData?.email??"",phone:initialData?.phone??"",address:initialData?.address??"",membership_status:initialData?.membership_status??"ACTIVE"});const[errors,setErrors]=useState<Partial<Record<keyof MemberCreate,string>>>({});function validate(){const e:typeof errors={};if(!form.full_name.trim())e.full_name="Full name is required";if(!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(form.email))e.email="Valid email is required";if(form.phone&&form.phone.replace(/\D/g,"").length<7)e.phone="Enter a valid phone number";setErrors(e);return Object.keys(e).length===0}async function submit(e:React.FormEvent){e.preventDefault();if(validate())await onSubmit(form)}function set(field:keyof MemberCreate,value:string){setForm(f=>({...f,[field]:value}));setErrors(er=>({...er,[field]:undefined}))}return <form onSubmit={submit} className="space-y-6"><div className="card p-6"><h2 className="text-base font-bold text-slate-950">Member profile</h2><p className="mt-1 text-sm text-slate-500">Contact and membership information used in lending workflows.</p><div className="mt-6 grid grid-cols-1 gap-5 md:grid-cols-2"><div><label className="label">Full Name *</label><Input value={form.full_name} onChange={e=>set("full_name",e.target.value)} placeholder="John Doe" />{errors.full_name&&<p className="field-error">{errors.full_name}</p>}</div><div><label className="label">Email *</label><Input type="email" value={form.email} onChange={e=>set("email",e.target.value)} placeholder="john@example.com" />{errors.email&&<p className="field-error">{errors.email}</p>}</div><div><label className="label">Phone</label><Input type="tel" value={form.phone} onChange={e=>set("phone",e.target.value)} placeholder="+91 98765 43210" />{errors.phone&&<p className="field-error">{errors.phone}</p>}</div><div><label className="label">Membership Status</label><Select value={form.membership_status??"ACTIVE"} onChange={e=>set("membership_status",e.target.value as MembershipStatus)}><option value="ACTIVE">ACTIVE</option><option value="INACTIVE">INACTIVE</option></Select></div><div className="md:col-span-2"><label className="label">Address</label><textarea className="input min-h-24 resize-y" value={form.address} onChange={e=>set("address",e.target.value)} placeholder="Street, city, state" /></div></div></div><div className="flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><Button type="button" variant="secondary" onClick={()=>history.back()}>Cancel</Button><Button type="submit" disabled={loading}>{loading?"Saving…":submitLabel}</Button></div></form>}
