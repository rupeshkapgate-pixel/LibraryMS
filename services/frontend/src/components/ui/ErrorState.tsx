import { AlertTriangle } from "lucide-react";
export function ErrorState({title="Unable to load data",message="Please check the API gateway and try again.",action}:{title?:string;message?:string;action?:React.ReactNode}){return <div className="flex flex-col items-center justify-center px-6 py-16 text-center"><div className="mb-4 rounded-2xl bg-red-50 p-4 text-red-500 ring-1 ring-red-100 dark:bg-red-500/10 dark:text-red-300 dark:ring-red-500/20"><AlertTriangle size={34}/></div><h3 className="text-sm font-semibold text-slate-900 dark:text-white">{title}</h3><p className="mt-1 max-w-sm text-sm text-slate-500 dark:text-slate-400">{message}</p>{action&&<div className="mt-5">{action}</div>}</div>}
export default ErrorState;
